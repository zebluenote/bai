{% extends 'base.html.twig' %}

{% block title %}
  Login
{% endblock %}

{% block body %}
  <div class="container">
    <div class="row">
      <div class="col-md-3">
        &nbsp;
      </div>
      <div class="col-md-6">
        <div class="bg-light py-3 px-3">
          <h1>
            Identification
          </h1>
          {% if hasError %}
              <div class="alert alert-danger">Impossible de vous identifier avec ces informations. Veuillez vérifier votre adresse email et votre mot de passe.</div>
          {% endif %}
          <form action="{{ path('account_login') }}" method="post">
            <div class="form-group">
              <label for="email">
                Email
              </label>
              <input type="text"
                required 
                name="_username"
                id="email"
                class="form-control"
                value="{{ lastUsername }}"
                placeholder="Adresse email..." />
            </div>
            <div class="form-group">
              <label for="password">
                Mot de passe
              </label>
              <input type="password"
                required 
                name="_password"
                id="password"
                placeholder="Mot de passe..."
                class="form-control" />
            </div>
            <div class="form-group">
              <button class="btn btn-success">
                Identification
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
{% endblock %}
